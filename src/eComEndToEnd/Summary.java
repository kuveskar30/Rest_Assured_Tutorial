package eComEndToEnd;

public class Summary {
	//1. on login to ecom app we are getting user_id and Token
	//2. on subsequent api calls we are using user id in body,
	//and token(Authorization=token or bearer=token) in headers for
	//authentication
	//3. we can send input from body as form-data by selecting form-data option
	//instead of json, data is sent as key value pair and,
	// it allows to send file attachment also
	
}
