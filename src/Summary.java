import io.restassured.RestAssured;

public class Summary {

	//What is API,WebServices?
	//Basic usage of postman for performing CRUD operations
	//http request methods:- get,post,put,delete
	//get - no body is sent, only query or path parameters are sent
	
	//post,delete,put info:-
	//post is used for put and delete also
	//body is sent in post,put,delete
	//we need to specify content type in header
	//for multiple query parameters or headers use these methods multiple times
	
	//given(),when(),then() --import manually as these come from static package
	//JsonPath used for performing operations on json object string
	//traversing through json path
	//sending path parameters from restAssured
	//SessionFilter
	//extract().response().asString();
	//RestAssured.baseURI = "https://rahulshettyacademy.com"
	//working with complex json
	

	
	
	
	
}
